const {describe, it} = global;
import {expect} from 'chai';
// import {shallow} from 'enzyme';
// import NotAuthCheck from '../not_auth_check';

describe('app.components.login', function () {

  // As a user, I want to be able to log in
  // So I need to see a form with two inputs : email and password
  // And I also need to see a submit button
  // If there is an error, I need to see it correctly

  it('should do something', function () {
    expect(true).to.equal(true);
  });
});
