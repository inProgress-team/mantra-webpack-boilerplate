const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import NotAuthCheck from '../not_auth_check';

describe('app.components.not_auth_check', () => {
  it('should do something', ()=>{
    expect(true).to.equal(true);
  });
});
